import { Controls } from '../../components/pageComponents/Home';
import { MainLayout } from '../../layouts';

const Home = () => {
  return (
    <MainLayout>
      <Controls />
    </MainLayout>
  );
};

export default Home;
